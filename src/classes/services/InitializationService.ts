import { WebSocketService } from './WebSocketService'

class InitializationService {
  public init = () => {
    // WebSocketService.init()
  }
}

const initializationService = new InitializationService()
export { initializationService as InitializationService }
